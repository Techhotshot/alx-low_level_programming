#include <stdio.h>

/**
 * main - this function prints all single digits
 *
 * Return: this prints 0 when successful
 */
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
